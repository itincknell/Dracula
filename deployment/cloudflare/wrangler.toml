name = "dracula-route"
main = "worker.mjs"
compatibility_date = "2026-09-07"
workers_dev = false

# Only the game path is intercepted; other pages retain their GitHub origin.
routes = [{ pattern = "ian-tincknell.com/Dracula*", zone_name = "ian-tincknell.com" }]

[vars]
API_ORIGIN = "https://3ylzpjexng.execute-api.us-east-1.amazonaws.com"
