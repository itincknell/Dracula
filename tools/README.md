# Tools

Active operational commands live in the package CLI and the root `Makefile`.
`archive/` contains one-off experiment and report utilities retained for
historical reproducibility; they are not part of the active command surface.
