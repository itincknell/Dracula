# Historical configurations

These configurations reproduce superseded learning experiments:

- `expert-iteration/`: single-model expert-iteration prototypes.
- `search-guided/`: search-guided policy/value warm-start training.
- `teacher-v2/`: shallow Teacher v2 distillation and response-ranking runs.

Nothing in this directory is selected implicitly by local gameplay or the
active Sam-32 corpus miner.
