# Configurations

`bgc-policy-pi1.toml` is the retained training configuration for the selected
standalone policy. Completed collection configurations live with ignored
sealed runs and their reports; retired experiment configurations are available
from Git history.

Local gameplay and release builds use the root `Makefile`. The installed
training command is `dracula-bgc-policy-training`.
