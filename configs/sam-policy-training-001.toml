format_version = "dracula-sam-policy-training-config-v1"

[run]
run_id = "sam-policy-training-001"
root_seed = "sam-policy-training-001-root-v1"
output_directory = "runs/sam-policy-training-001"

[dataset]
snapshot_path = "runs/sam-policy-snapshot-001/corpus-snapshot.json"

[model]
model_id = "sam-policy-001"
initialization_ordinal = 0

[optimization]
device = "cpu"
minimum_epochs = 8
maximum_epochs = 50
early_stop_patience = 5
minimum_improvement = 0.0001
