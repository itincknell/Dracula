# Post-cleanup repository validation

Validation date: July 25, 2026

## Result

The cleaned repository is mechanically consistent. Full Python and frontend
verification passed, package metadata is valid, documentation and report links
resolve, archives remain readable, the SQLite game record is intact, and the
continuous Sam-32 collector remains isolated and healthy.

No source, contract, test, archive, database, frozen collector file, or corpus
artifact required repair. Validation-generated bytecode, pytest state, package
metadata, wheel output, and restoration scratch files were removed afterward.

## Commands

Core and frontend verification:

```bash
nice -n 10 .venv/bin/python -m pytest
nice -n 10 npm --prefix frontend run check
```

Static imports and package verification:

```bash
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python - <<'PY'
import importlib
import pkgutil
import dracula

modules = sorted(
    module.name
    for module in pkgutil.walk_packages(
        dracula.__path__,
        dracula.__name__ + ".",
    )
)
for name in modules:
    importlib.import_module(name)
print(len(modules))
PY

.venv/bin/pip check
nice -n 10 .venv/bin/pip wheel . \
  --no-deps \
  --no-build-isolation \
  --wheel-dir .local/repo-hygiene-validation-build
```

Command-surface verification:

```bash
make help
for opponent in \
  search search-v2 search-v2-nested \
  search-v2-student-direct search-v2-student-top-2 \
  search-v2-student-top-3 guided archive; do
  make check-opponent OPPONENT="$opponent"
done

for target in \
  dev preview preview-control dev-api dev-frontend \
  play-report reset-local test test-web test-e2e \
  corpus-health corpus-inspect corpus-verify corpus-stop corpus-resume; do
  make -n "$target"
done
make -n play-game \
  GAME_ID=00000000-0000-0000-0000-000000000000

.venv/bin/dracula-sam-miner --help
for command in \
  initialize smoke mine resume inspect verify summarize; do
  .venv/bin/dracula-sam-miner "$command" --help
done
```

Corpus and source-isolation verification:

```bash
nice -n 10 make corpus-inspect
nice -n 10 make corpus-verify

cd .local/collector-source/sam-32-continuous-corpus-001
PYTHONPATH="$PWD/src" \
  ../../../.venv/bin/python -c \
  'from dracula.sam_miner import resolve_source_identity; print(resolve_source_identity())'
```

Archive verification:

```bash
nice -n 15 node <<'NODE'
const crypto = require("crypto");
const fs = require("fs");
const manifestPath = ".local/archive/2026-07-25/manifest.json";
const manifest = JSON.parse(fs.readFileSync(manifestPath, "utf8"));
const manifestDigest = fs
  .readFileSync(`${manifestPath}.sha256`, "utf8")
  .trim()
  .split(/\s+/)[0];
const items = [
  { path: manifestPath, digest: manifestDigest },
  ...manifest.archives.map((item) => ({
    path: item.archive_path,
    digest: item.archive_sha256,
  })),
];

(async () => {
  for (const item of items) {
    const hash = crypto.createHash("sha256");
    for await (const chunk of fs.createReadStream(item.path)) {
      hash.update(chunk);
    }
    if (hash.digest("hex") !== item.digest) {
      throw new Error(`digest mismatch: ${item.path}`);
    }
  }
})().catch((error) => {
  console.error(error.message);
  process.exit(1);
});
NODE

for archive in $(jq -r \
  '.archives[].archive_path' \
  .local/archive/2026-07-25/manifest.json); do
  nice -n 15 zstd -q -t "$archive"
done

zstd -q -dc \
  .local/archive/2026-07-25/runs/search-v1/search-v1-artifacts.tar.zst |
  tar -xf - -C .local/repo-hygiene-validation-restore \
  runs/search-validation-001/fixture-records.json
```

Database and public-response verification:

```bash
sqlite3 -readonly .local/dracula.sqlite3 'PRAGMA integrity_check;'
sqlite3 -readonly .local/dracula.sqlite3 < queries/local-game-report.sql
curl -fsS http://127.0.0.1:8000/health
game_id=$(sqlite3 -readonly .local/dracula.sqlite3 \
  'select game_id from games order by rowid desc limit 1;')
curl -fsS "http://127.0.0.1:8000/games/$game_id"
curl -fsS "http://127.0.0.1:8000/games/$game_id/events"
```

Documentation and diff verification:

```bash
node <<'NODE'
const fs = require("fs");
const path = require("path");
function walk(directory) {
  return fs.readdirSync(directory, { withFileTypes: true }).flatMap((entry) =>
    entry.isDirectory()
      ? walk(path.join(directory, entry.name))
      : [path.join(directory, entry.name)],
  );
}
const files = ["README.md", ...walk("docs"), ...walk("reports")].filter(
  (file) => file.endsWith(".md"),
);
const errors = [];
const slug = (heading) =>
  heading
    .trim()
    .toLowerCase()
    .replace(/[`*_~]/g, "")
    .replace(/[^\p{L}\p{N}\s-]/gu, "")
    .replace(/\s+/g, "-")
    .replace(/-+/g, "-");
for (const file of files) {
  const body = fs.readFileSync(file, "utf8");
  for (const match of body.matchAll(/\[[^\]]*\]\(([^)]+)\)/g)) {
    const target = match[1].trim();
    if (/^(https?:|mailto:|#)/.test(target)) continue;
    const separator = target.indexOf("#");
    const pathname =
      separator === -1 ? target : target.slice(0, separator);
    const anchor = separator === -1 ? "" : target.slice(separator + 1);
    const resolved = path.normalize(path.join(path.dirname(file), pathname));
    if (!fs.existsSync(resolved)) {
      errors.push(`${file}: ${target}`);
      continue;
    }
    if (anchor && fs.statSync(resolved).isFile()) {
      const headings = [
        ...fs.readFileSync(resolved, "utf8").matchAll(/^#{1,6}\s+(.+)$/gm),
      ].map((heading) => slug(heading[1]));
      if (!headings.includes(anchor)) errors.push(`${file}: ${target}`);
    }
  }
}
if (errors.length) {
  console.error(errors.join("\n"));
  process.exit(1);
}
NODE

if rg -ni \
  'version 1 remains the default|500.simulation planner is.*default|active (shallow )?teacher v2|active sam-128|collection (has not|hasn.t) started|one million example cap|begin collection only after|explicit user authorization' \
  README.md docs reports/active reports/README.md Makefile; then
  exit 1
fi

if rg -n \
  '\b(APP|ASSET|CONTRACT|EVAL|FE|MINER|MODEL|NARRATOR|OPS|RELEASE|SEARCH|SERVE|TRAIN)-[0-9]{3}\b' \
  README.md docs reports \
  --glob '*.md' \
  --glob '!docs/design-tracker.md'; then
  exit 1
fi
git diff --check
```

## Test results

| Check | Result |
| --- | --- |
| Python | 543 passed in 1,193.59 seconds |
| Frontend | 10 files and 73 tests passed |
| TypeScript | Passed |
| ESLint | Passed |
| Production build | Passed; no hard-coded local API URL |
| Static imports | Package root plus 47 discovered modules imported |
| Python dependencies | `pip check` found no broken requirements |
| Wheel | Built successfully; only `dracula-sam-miner` is exported |
| Markdown | 42 files; no missing path or heading anchor |
| Tracker IDs | Confined to `docs/design-tracker.md` |
| Stale active claims | None |
| Diff formatting | Passed |

The environment does not install the optional `build` package. Packaging was
therefore verified with `pip wheel`, using the existing PEP 517 backend without
adding a dependency.

## Archive verification

The archive manifest SHA-256 remains:

`4cb49be791937b7c68c8bd23fdcd809285e20ca3668212a0a4f107fe4248d006`

All 11 archive files match the digests recorded in the manifest. Every zstd
stream passed integrity testing.

The representative search-validation file
`runs/search-validation-001/fixture-records.json` was restored from the
search-v1 archive. Its 104,625 bytes reproduce SHA-256:

`acf65ecaf3a49068ba9f55397bf966bd63a0350dde8892c80afd77004b8c9c64`

The restoration scratch directory was removed after comparison.

## Disk use

| Area | Allocated size |
| --- | ---: |
| Local historical archive | 3,434,737,664 bytes |
| Active corpus and staging | 1,373,319,168 bytes |
| Python virtual environment | 754,991,104 bytes |
| Frontend dependencies | 153,378,816 bytes |
| Production frontend build | 487,424 bytes |
| SQLite main database | 34,668,544 bytes |
| SQLite WAL | 4,198,400 bytes |

The data volume had 126,914,108 KiB free, approximately 121 GiB. The active
collector's 1 GiB floor plus per-worker reserve remains far below available
space.

## Collector health

| Field | Result |
| --- | --- |
| Parent PID | 45590 |
| Search workers | Four, all running |
| Resource tracker | One |
| Working directory | Frozen collector checkout for parent and every child |
| Source identity | Revision and tree digest exactly match resolved configuration |
| Committed decks | 4 |
| Committed rows | 131,064 |
| Committed terminal leaves | 196,608 |
| Staging | Four deck directories, four sealed rounds each |
| Staging temporary or stop files | 0 |
| Parent and child RSS | About 118 MiB combined |
| System memory free | 40% |
| Swap at final check | 5,287.5 MiB used of 6,144 MiB |
| Free disk | Approximately 121 GiB |

Swap usage decreased by 40 MiB between the two health samples in this
validation; no growth was observed. Collector stdout and stderr remain attached
to `output-sam-32-continuous-corpus-001`. The log contains the documented
isolation interrupt and no traceback, corruption, hash, or disk error.
Subsequent batch output is pending completion of the four in-flight decks.

Frozen-source corpus inspection and verification both accepted:

```text
configuration digest:
d681a05fd1e9d53d07ee5e158166d47ccf55e1dbfb472910718712ee626caada

corpus content digest:
e56bb000a98dbd8da0e951f1c5a1aa3290b0173d97e5bb9e53cfd868d47c0603

24 rounds
120 committed shards
131,064 teacher-cache entries
131,064 rows
```

The recovery instructions contain the frozen absolute checkout and frozen
`PYTHONPATH`; no mutable `PYTHONPATH=src` recovery command remains.

## SQLite and privacy

`PRAGMA integrity_check` returned `ok`.

```text
games:                 25
completed games:       19
in-progress games:      6
public events:       1,610
idempotency records: 1,195
create requests:        25
```

The live health route and production frontend returned HTTP 200. A real stored
game view and event stream contained no stock, stock order, seed, opponent hand,
hidden state, logits, model path, model state, search tree, determinization, or
authoritative engine-state key.

The same forbidden-key scan returned zero matches across stored public events,
idempotency response bodies, and create response bodies. The sealed corpus
contained zero forbidden dataset fields. Full API and miner tests additionally
exercise response projection, privacy rejection, exact row schemas, and
artifact verification.

## Git and generated files

The cumulative working tree contains 33 modified tracked files, 30 tracked
deletions, and 14 untracked path groups. The scope is understandable:

- Active Sam, miner, API, and test implementation from the completed build
  stages.
- Historical configuration and tool moves into archive subdirectories.
- Historical report moves into the new report hierarchy.
- Documentation and Makefile cleanup.
- Frontend test and Vampire-attribution corrections.

No untracked cache, bytecode, test result, package metadata, wheel, restoration
scratch, or accidental generated file remains. `frontend/dist` remains because
it is the active production preview output documented by the archive pass.

## Retained historical complexity

Historical PPO, original-search, shallow Teacher v2, response-ranking,
guided-search, expert-iteration, and tournament readers remain in
`src/dracula`. They are retained because explicit comparison controllers,
archived artifact inspection, and regression tests still import them.

Historical configuration and report paths are separated under `archive/` and
`reports/history/`; they are not installed commands or active defaults. Stable
schema identifiers that include older names remain unchanged because renaming
them would break artifact compatibility.

## Unresolved defects

None.
